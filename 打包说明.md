# 打包说明

当前版本：**V1.0.2**  
更新日期：**2026-06-09**

## 固定打包环境

本项目当前使用的本地 Python/打包环境：

- Python：`D:\D\Python311\python.exe`
- 虚拟环境：`D:\D\PythonBuildEnv\venvs\genpao-browser`

如果环境不存在，先创建：

```powershell
D:\D\Python311\python.exe -m venv D:\D\PythonBuildEnv\venvs\genpao-browser
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m pip install --upgrade pip
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m pip install -r requirements.txt
```

## 打包前检查

每次准备上传或发布前，先同步更新：

1. `main.py` 顶部版本说明。
2. `README.md`。
3. `打包说明.md`。
4. `操作说明.txt`。
5. `requirements.txt`（如果依赖变化）。
6. `跟跑助手.spec`（如果打包配置变化）。

确认所有说明都更新后，再执行打包和 zip。

## 打包 exe

```powershell
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m py_compile main.py
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m PyInstaller --clean --noconfirm 跟跑助手.spec
```

打包完成后，exe 位于：

```text
dist\跟跑助手.exe
```

将新版 exe 复制到根目录：

```powershell
Copy-Item .\dist\跟跑助手.exe .\跟跑助手.exe -Force
```

## 制作正式版 zip

正式版 zip 名称：

```text
跟跑浏览器正式版（pc 金金村专用 群号：950610825）.zip
```

zip 内只放：

```text
跟跑助手.exe
hotkeys.json
操作说明.txt
```

不要放：

- `data/`：WebView2 用户数据，可能包含登录态/Cookie/历史记录。
- `debug.log`：本地运行日志。
- `build/`、`dist/`：构建中间目录。
- 源码文件：正式版只给用户运行，不放开发源码。

## GitHub 上传规则

上传 GitHub 前确认：

- `.gitignore` 已排除 `data/`、`*.exe`、`*.zip`、`debug.log`、`build/`、`dist/`。
- `git status --ignored` 中这些内容显示为 ignored。
- 用户已确认目标仓库名和公开/私有可见性。

## V1.0.2 打包结果

- 新版 `跟跑助手.exe`：约 16.78MB。
- 正式版 zip：约 16.52MB。
