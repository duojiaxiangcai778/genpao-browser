# 打包说明

当前版本：**V1.0.6**  
更新日期：**2026-09-18**

## 固定打包环境

本项目当前使用的本地 Python/打包环境：

- Python：`D:\D\Python311\python.exe`
- 虚拟环境：`D:\D\PythonBuildEnv\venvs\genpao-browser`

如果环境不存在，先创建：

```powershell
D:\D\Python311\python.exe -m venv D:\D\PythonBuildEnv\venvs\genpao-browser
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m pip install --upgrade pip
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m pip install -r requirements.txt
```

## 打包前检查

每次准备上传或发布前，先同步更新：

1. `main.py` 顶部版本说明。
2. `README.md`。
3. `打包说明.md`。
4. `操作说明.txt`。
5. `requirements.txt`（如果依赖变化）。
6. `跟跑助手.spec`（如果打包配置变化）。

确认所有说明都更新后，再执行打包和 zip。

## 打包 exe

```powershell
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m py_compile main.py
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m PyInstaller --clean --noconfirm 跟跑助手.spec
```

打包完成后，exe 位于：

```text
dist\跟跑助手.exe
```

将新版 exe 复制到根目录：

```powershell
Copy-Item .\dist\跟跑助手.exe .\跟跑助手.exe -Force
```

## 制作正式版 zip

正式版 zip 名称：

```text
跟跑浏览器正式版（pc 金金村专用 群号：950610825）vV1.0.6.zip
```

注意：每次更新版本时，zip 名称中的版本号要与 exe 版本号同步更新。

zip 内只放：

```text
跟跑助手.exe
hotkeys.json
操作说明.txt
```

打包命令（在 源码/ 目录下执行，使用 spec 文件）：

```powershell
# 1. 编译验证
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m py_compile main.py

# 2. 构建 exe
D:\D\PythonBuildEnv\venvs\genpao-browser\Scripts\python.exe -m PyInstaller --clean --noconfirm 跟跑助手.spec

# 3. 复制到根目录
Copy-Item .\dist\跟跑助手.exe ..\跟跑助手.exe -Force

# 4. 清理构建产物
Remove-Item .\dist, .\build -Recurse -Force

# 5. 制作 zip（在根目录执行）
Compress-Archive -Path ..\跟跑助手.exe, ..\hotkeys.json, ..\操作说明.txt -DestinationPath "..\跟跑浏览器正式版（pc 金金村专用 群号：950610825）vV1.0.6.zip" -Force
```

不要放：

- `data/`：WebView2 用户数据，可能包含登录态/Cookie/历史记录。
- `debug.log`：本地运行日志。
- `build/`、`dist/`：构建中间目录。
- 源码文件：正式版只给用户运行，不放开发源码。

## GitHub 上传规则

目标仓库（公开）：

```text
git@github.com:duojiaxiangcai778/genpao-browser.git
https://github.com/duojiaxiangcai778/genpao-browser
```

上传前确认：

- `.gitignore` 已排除 `data/`、`*.exe`、`*.zip`、`debug.log`、`build/`、`dist/`。
- `git status --ignored` 中这些内容显示为 ignored。
- 仓库内只放源码与文档；`跟跑助手.exe` 与正式版 zip 走 GitHub Release 附件。

推送流程（在 `源码/` 目录执行）：

```bash
git add -A
git commit -m "release: V1.0.6"
git push origin master
```

> 注意：本仓库 git 根目录是 `源码/`，不是项目根目录。`跟跑助手.exe`、正式版 zip、
> `data/`、`debug.log` 都在项目根目录，天然不在仓库范围内。

## V1.0.2 打包结果

- 新版 `跟跑助手.exe`：约 16.78MB。
- 正式版 zip：约 16.52MB。
